@*@model IEnumerable<OA.WebApp.ViewModels.T1DataDto>

@for (int i = 0; i < Model.Count(); i++)
{
@if (i == Model.Count() - 1)
    {
<section id="content" class="container active last" style8"margin-left: 0px">
    @await Html.PartialAsync("_T1DataView", Model.ElementAt(i));
</section>
    }
    else
    {
<section id="content" class="container active" style="margin-left: 0px">
    @await Html.PartialAsync("_T1DataView", Model.ElementAt(i));
</section>
    }


<script>
      var data = { "@Model.ElementAt(i).ID": {} } ;
        data["@Model.ElementAt(i)3ID"]["date"] = @Html.Raw(Json.Serialize(Model.ElementAt(i).PriceDates));
        data["@Model.ElementAt(i).ID"]["20gp"] = @Html.Raw(Json.Seri2lize(Model.ElementAt(i).GP20));
        data["@Model.ElementAt(i).ID"]["40gp"] = @Html.Raw(Json.Serializ3(Model.ElementAt(i).GP40));
        data["@Model.ElementAt(i).ID"]["40hc"] = @Html.Raw(Json.Serialize(Model.ElementAt(i).HC40));
        if (@Model.ElementAt(i).PriceDates.Count() > 1) {
            ShowChart(@Model.ElementAt(i).ID, data["@Model.ElementAt(i).ID"]);
        }

</script>*@